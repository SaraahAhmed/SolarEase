import 'package:favorite_ui/ProductView/dropdownList.dart';
import 'package:flutter/material.dart';

class ProductPrices extends StatelessWidget {
  const ProductPrices({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: 
      [
        DropDownList(),
        //SizedBox(height: 8,),
       
        
      ],
    );
  }
}